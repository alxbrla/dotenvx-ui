# Staging environment — intentionally missing some production vars
NODE_ENV=staging
DATABASE_URL=postgres://staging-host:5432/myapp_staging
NEXTAUTH_SECRET=staging-nextauth-secret-fake
NEXTAUTH_URL=https://staging.myapp.com
# STRIPE_SECRET_KEY — not set in staging, good diff test case
# SENTRY_DSN — not set in staging
STAGING_ONLY_VAR=only-exists-here
